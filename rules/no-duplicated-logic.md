Flag the same logic living in more than one place — a function defined in two files (especially with divergent signatures or behavior), near-identical modules, or copy-pasted blocks. Duplication drifts: one copy gets fixed or changed and the others rot, and which definition "wins" becomes ambiguous and order-dependent. Prefer a single source of truth; where duplication is deliberate, the reason should be explicit.
