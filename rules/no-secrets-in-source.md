Secrets — passwords, API keys, tokens, connection strings with embedded credentials — must never be hardcoded in version-controlled source; read them from environment variables, a secrets manager, or the platform credential store. Flag any literal credential, and scrutinize functions that retrieve a password, token, or key to confirm they resolve from a secure source rather than a baked-in or committed value.
