The pre-push hook's findings pointer (arc log --last --command <verb>) is ambiguous on a multi-repo ledger: another repo's newer run shadows this repo's. The pointer should carry --repo <repo> (and the hook knows its repo).
