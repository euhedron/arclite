Make each audit's scope first-class and structured — the files it read, the rules' share, the commits judged — and derive from it per-repo freshness (change accumulated since the last audit's commit) and velocity (change inflow versus audit and finding outflow over time), surfaced cross-repo with configurable thresholds that warn when accumulated change crosses them — the one threshold-and-warn facility, which other metrics reuse rather than rebuild. Includes gate-round linkage: a push's audit rounds visible as one sequence, which needs a round marker the hook stamps.
---

Resolved by split, not by completion: audit-scope-record (the structured substrate), repo-drift-metrics (freshness + velocity, and the one threshold-and-warn facility), and gate-round-linkage (the hook-stamped round id) — each with a definable done.
