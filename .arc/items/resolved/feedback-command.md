A feedback channel, CLI and TUI — one verb, two sinks. Outbound (any user or agent → Euhedron): capture a report — what happened, and the run id it concerns — locally under `~/.arc` until a euhedron endpoint exists to route it; collection first, transport later (a prefilled GitHub issue is the zero-infrastructure interim, the rulespace registry the eventual real sink). Local inbox (operator → future sessions): queue ideas and corrections where the next session reads and triages them — the agenda's lighter-weight cousin, and another source the advisory runs can audit. TUI surfaces for both.

---
Resolved: landed as `arc feedback` plus the TUI feedback view — one verb, two sinks: the outbound queue at `~/.arc/feedback.jsonl` (prefilled GitHub issue as the interim transport; the endpoint rides rulespace-registry) and the repo inbox at `.arc/inbox/` (README-seeded, one file per note, triaged by the next session).
