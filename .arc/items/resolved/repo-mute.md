Per-repo mute: disable a retired repo across arc's surfaces — the ledger lenses, status, and log, CLI and TUI alike — so ended experiments stop appearing without deleting their history. A muted repo stays in the ledger (the record is permanent); mute is a view-level default, disclosed wherever it filters. Setter and storage: a `muted_repos` settings key on the `disabled_rules` pattern — `arc config set muted_repos <paths>`, toggleable in the TUI's repo lenses — user-layer only, since a mute is the operator's lens over the operator's ledger (the mechanics-are-private doctrine), never a repo's tracked claim about itself.

---
Resolved: `muted_repos` lands as a plain (non-secret) user-layer-only setting — the loader rejects a project layer, `config set` elevates, and the secret/user-layer-only concepts split so a mute is settable inline and shown unmasked. One filter home (`log::split_muted`, exact recorded paths); applied with disclosure on every default view — `arc usage` (both rollups' notes), `arc log` list/--last, the TUI status tail ("N muted") and usage lens cycle — and bypassed by any explicit selection (--repo, a repo lens, the launch cwd). Unreadable settings fail open with disclosure where a note channel exists. Verified end-to-end: set → disclosures on usage/log/status → bypass shows the muted repo → unset.
