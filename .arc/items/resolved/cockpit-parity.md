Close the cockpit's product-surface gaps — one theme, several small surfaces: a TUI init flow, a feedback command (CLI and TUI, captured locally until a euhedron endpoint exists), a release-notes view from the Releases bodies `arc update` already reads, and built-in ruleset discoverability (the rules surfaces name `default` as available instead of dead-ending at an empty state). Each surface buildable independently.

---

Resolved by split, not by completion: the four surfaces are their own items — ruleset-discoverability, tui-init, feedback-command, release-notes-view — so each resolves on its own instead of this basket lingering open until its last member lands.
