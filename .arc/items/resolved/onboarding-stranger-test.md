Prove the stranger path: a downloaded binary, no sibling context, no hand-holding — the README plus `arc doctor` plus `arc init` carrying a new user end to end through extract, curate, and audit on their own repo. Walk it cold, fix what snags, keep the walk repeatable.

---
Resolved: walked end-to-end as a stranger (isolated HOME, fresh repo): help → init → doctor → rules → the must-choose backend error → dry-run → first real audit (caught the planted flaw, declined the non-violation, $0.16). Five edges found, all fixed same-branch: the self-referential crate description; init now orients (relative paths, the active built-in ruleset, next steps); doctor's failed cargo probe reads as the from-source-only concern it is; dry runs print the prompt first and the human digest last, so the terminal's visible tail is the summary; the README's audit example carries --include src. The hook's findings pointers gained --repo along the way (the pending inbox note's catch — triaged and cleared).
