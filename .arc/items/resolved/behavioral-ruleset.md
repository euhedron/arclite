Seed a behavioral ruleset — reasoning anti-patterns for judging agent conduct rather than code: the README's exhibit list restated as curated rules under the established curation bar. Distinct from the code rules, composed like any ruleset; its firing record is what keeps the list honest. Drafted and rule-quality-clean: the ten exhibits stand as `.arc/behavioral-rules`, the `behavioral` ruleset — defined, selected by nothing. What remains cannot be pre-validated: rules prove themselves only in live exercise, so the remainder is operator curation plus the live firing record (the loop live-transcript-auditing builds).

---
Resolved: the seed is whole — eleven rules in `.arc/behavioral-rules` (the README's ten exhibits plus `use-visible-change-surfaces`, caught live by the operator mid-draft), the `behavioral` ruleset defined and selected by nothing, rule-quality-clean. The remainder was never this item's to build: curation is the operator's pass, and the live firing record is live-transcript-auditing's loop.
