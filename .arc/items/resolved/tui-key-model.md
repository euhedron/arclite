Converge the TUI's key model on the bar the agent-CLI cockpits set: arrows, enter, and escape carry primary navigation and submenu movement; letter keys only where they are the logical choice, always labeled, rarely a function's only path. Today's sole-path letters (the usage view's lens and page keys) move to arrow-navigable tabs; data lands in aligned columns with disclosures as compact dim facts. TUI surfaces built after this item follow its bar — the policy lives here, once, not restated per surface.

---
Resolved: the sole-path letters are gone — the usage pages are ←→ tabs (each header names the active page with its sibling dimmed behind the arrow that reaches it), and the repo lens is arrow-pathed on the spend page (↑↓, which scrolls nothing there) while keeping the labeled `p` on the firing page where ↑↓ scrolls; `r` removed. Remaining letters are labeled, logical-choice accelerators (rules' space toggle, feedback's n/i), per the bar's own carve-out. Aligned columns and dim-fact disclosures landed with the earlier usage/rules passes. The bar for surfaces built later stays stated here.
