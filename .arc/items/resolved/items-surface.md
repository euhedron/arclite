Surface the agenda ambiently, beyond the landed read surfaces (`arc items` and the TUI items view): a doctor line (item counts + order integrity), a TUI masthead warning when integrity is broken, and pending inbox notes surfaced the same way — so drift and queued notes are seen without asking.

---
Resolved: `arc doctor` carries the items row (open/resolved counts, order integrity, pending-inbox count — zero-pending silent; absent agenda as the compact fact; unreadable states their own, never empty), and the TUI masthead warns at launch on agenda drift, pending inbox notes, or an unreadable agenda/inbox — the cwd-note posture (startup-computed, render pure). Verified across healthy, absent, drifted, and pending states in a scratch repo.
