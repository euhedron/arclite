Make each audit's scope first-class and structured — the files it read, the rules' share, the commits judged — and derive from it per-repo freshness (change accumulated since the last audit's commit) and velocity (change inflow versus audit and finding outflow over time), surfaced cross-repo with configurable thresholds that warn when accumulated change crosses them. Includes gate-round linkage: a push's audit rounds visible as one sequence, which needs a round marker the hook stamps. Also rides here: context-window utilization — the share of a model's window the rules + content occupy, in previews and views, under the same thresholds.
