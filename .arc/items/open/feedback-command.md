A feedback command, CLI and TUI: capture a report — what happened, and the run id it concerns — locally under `~/.arc` until a euhedron endpoint exists to route it. Collection first, transport later.
