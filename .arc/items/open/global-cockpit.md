`arc tui` launched anywhere — the home folder included — is the device-wide cockpit, not a mistake: the masthead's home-folder warning stays right for runs (whose context would scan the tree), but the cockpit reads discovery and the ledger, so the global launch is the point — the launch-cwd-as-default-lens principle completed. The home view: every arc-enabled project on the device as rows — device-awareness's map joined with the ledger — showing per repo the rules count, items open and resolved, whether it is gated and which hooks are enabled, spend total and today, recency of the last audit, commits since the last gated push, and rounds per push (gate-round-linkage's unit); selecting a row opens the repo's detail — the evolution over time of commits versus audit rounds versus blocked/passed outcomes, with averages (rounds per push, time per gate). Cross-project alerting surfaces here: agenda drift in any repo, thresholded measures through repo-drift-metrics' facility (context-window share, item counts at zero or extreme, disabled-rule counts, dirty-file counts), and which project's sessions are waiting on the operator (activity recency from the discovery map). The aggregate read the view exists for: the degree to which arc is impacting development across every repo — the stability and rate of change of rules, items, and order, and the loop's economics from the ledger's history (spend per resolved item, per gate round, and their trends: the valuemaxxing measure — whether the loop's compounding output pays for its spend). Consumes device-awareness, gate-round-linkage, repo-drift-metrics, and context-window-utilization; what is new here is the assembled home surface and its alert wiring.
