The usage view's navigation, reshaped by operator use. A tab bar in the masthead shows every page with the active one highlighted (`spend │ firing`), replacing the `(→ firing)` breadcrumb — the pattern any paged view adopts as it gains pages. Repo selection becomes a type-to-filter picker on `p` (the `/` palette's idiom pointed at the lens list), replacing blind cycling — twenty discovered repos made cycling the wrong shape — and `p` works identically on both pages (today spend cycles repos with `↑↓` while firing displaces repo onto `p`: two keys for one concept across tabs of one view). `↑↓` stays scroll where content scrolls.
