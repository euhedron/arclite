Projects declare deterministic requirements the gate checks before any AI step spends: structural assertions about the repo's own shape — an items ledger exists, perhaps non-empty; a rules directory; whatever the project's discipline demands — configured in settings and failing the push by naming the unmet requirement (the operator's ask, 2026-09-16). Free and instant, so they run first: an AI round never spends against a repo missing its own declared preconditions. Rides gate-configuration's configured gate — requirements become one more declared step kind in the same composition the hook shim and CI both read. Distinct from rules (standards judged over content) and align (agenda coherence): a requirement is a structural fact checkable without judgment.
