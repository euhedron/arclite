Derive per-repo freshness (commits/files/lines changed since the last audit's commit — deterministic, from the record plus git) and velocity — per time window (a week, say): commits landed versus audit *rounds* completed (gate-round-linkage's unit, which precedes this item: a heavy push is many runs but one round) and findings raised/resolved, read from git plus the run record; an inflow/outflow pairing, not a single scalar. Surfaced cross-repo by extending device-awareness's per-repo rows (that item precedes this one and owns the discovery and the status seat's device rows; this one adds the git/ledger figures to those rows — and to the home rows once global-cockpit assembles them). Establishes the one configurable threshold-and-warn facility that thresholded numeric metrics reuse — distinct from the cockpit's boolean status flags, which need no thresholds and already ship (backend-unset in the footer, order integrity in the masthead). Builds on the run record's landed scope fields (files/lines/chars as entered, the rules and ledger shares — `scope` on every record). The change scale surfaces where runs are read, too: a run-history row carries the size of the change its run covered, so scale is visible per run and not only per repo (the operator's ask, 2026-09-09).
