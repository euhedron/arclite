Close the cockpit's product-surface gaps — one theme, several small surfaces: a TUI init flow, a feedback command (CLI and TUI, captured locally until a euhedron endpoint exists), a release-notes view from the Releases bodies `arc update` already reads, built-in ruleset discoverability (the rules surfaces name `default` as available instead of dead-ending at an empty state), and a direct-API backend considered under the membership criterion (natively structured output, no agent CLI). Each buildable independently; euhedron login lives with rulespace-registry, whose accounts it presupposes.
