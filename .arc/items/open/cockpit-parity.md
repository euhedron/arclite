Close the cockpit's product-surface gaps: a TUI init flow, a feedback command (CLI and TUI, captured locally until a euhedron endpoint exists), a release-notes view from the Releases bodies `arc update` already reads, euhedron login beside the provider API keys once the sibling auths unify, and a direct-API backend considered under the membership criterion (natively structured output, no agent CLI). Also built-in ruleset discoverability: the rules surfaces name `default` as available instead of dead-ending at an empty state.
