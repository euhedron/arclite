Build the rulespace registry: a remote shared home for rules and rulesets with accounts and organizations, publishing and sharing, and aggregated firing data — so curation pressure compounds across users, projects, and organizations instead of one machine. Core before surfaces: the registry itself, then arc in GitHub Actions/PR checks and auditing any public repo from a web surface, then editor extensions — and arc-side euhedron login (accounts beside the provider API keys) once those accounts exist. Its own repo under Euhedron; provenance/lineage of rules across versions and trust/moderation of shared rules are its open design questions.
