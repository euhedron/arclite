A lexicon: canonical project terms and their casing, enforced so naming drift in product and repo names is auto-caught rather than hand-policed. First shape: a JSON pack of `{term, casing}` composed like any rule source — checked deterministically where exact matching suffices, AI judgment only for context-dependent drift — seeded with Euhedron's own names (Euhedron, arclite, Arc, crux, commissure, rulespace).
