How many findings a run raised, visible wherever runs are listed: the status view's recently-completed tail, `arc log`'s rows, and the run detail show outcome and cost but not the count — blocked-with-one and blocked-with-five read identically until the stored result is opened (the operator's ask, 2026-09-16). The count reads from the run's stored structured result — the stored-results split the usage rules rollup already draws — and a record whose result is absent or unreadable shows unknown, never zero. context-window-utilization's strip consumes the same per-run count for its findings-per-run segment; this item gives the count one home across the listing surfaces.
