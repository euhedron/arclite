Finish making a verb a configuration: its role directive, taxonomy, item schema, and gate field shipped as built-in defaults and overridable through settings like everything else, so a new verb needn't mean a rebuild. Landed so far: taxonomies are settings-configurable, and each verb's role is a data string on its registry row (no verb-specific phrasing hides in code). Remaining, in order: the role string becomes an operator-editable Markdown file on the rules substrate (settings-overridable, like a rule); then the item schema and the gate field as data.
