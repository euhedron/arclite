Verify releases on machines that aren't ours: a post-release workflow that downloads each published binary onto fresh per-OS runners and exercises `doctor`, `init`, and `--version` — and exercises `arc update --apply` from the previous release — so a broken asset is caught by ephemeral clean machines instead of a user. The same workflow should exercise every distribution channel — the install scripts and the Homebrew tap — not just the raw assets. Remaining residue rides this item too: further registries (crates.io — operator's call — cargo-binstall, Scoop/WinGet) and signed releases.
