Distribution residue beyond the landed channels and verifier: further registries — crates.io (today deliberately gated off by the manifest's `publish = false`; flipping it is the operator's call), cargo-binstall, Scoop/WinGet — and signed releases (out-of-band key or build attestations). Keep `verify-release.yml` green per release and extend it as channels are added.
