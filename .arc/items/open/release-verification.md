Distribution residue beyond the landed channels and the landed post-release verifier (`verify-release.yml`: binaries checksum-verified and exercised on fresh per-OS runners, install scripts, tap, update-from-previous — proven against v0.1.12): further registries (crates.io — operator's call — cargo-binstall, Scoop/WinGet) and signed releases (out-of-band key or build attestations). Keep the verifier green per release; extend it as channels are added.
