The items view reorders the agenda in place: select an item, move it to a new position, the write landing in order.json and the view re-reading through the same loader `arc items` and a run's agenda context use — shown state is the persisted order, never assumed from the edit. Today the intended order changes only by hand-editing order.json; the cockpit shows order and drift but can't act on either. An order forces a choice, and the cockpit making that choice cheap is the point; membership changes (add, resolve) stay with their own commands.
