Extract the verb prompts — today inline in Rust (the per-verb role directives, plus the shared assembly: the grounding guardrail, the structured/ranked notes, the taxonomy block) — into dedicated Markdown assets embedded into the binary the way `.arc/rules` are, so a prompt is edited and reviewed as prose, not recompiled. This makes concrete the roles-as-Markdown step the verbs-as-configurations question names. Then add a dedicated **prompt-quality** meta-ruleset and a pre-push meta-gate that audits the prompts against it when they change — the exact apparatus `rule-quality` already has over `.arc/rules`, dedicated for the same reason: an inline prompt reads as a *string* to the code audit, and prompt-quality is a different intelligence than code-quality (DRY, no-hardcoding). Seed prompt meta-rule (surfaced in a crux session, 2026-08-23): a prompt names the role and the grounds and stops — it does not prescribe the tools, the reasoning, or the thinking-process the model is meant to own; over-instruction usurps the judgment arc's premise reserves for the model, and is brittle across model versions. Prompts are first-class curated artifacts — their articulation is as significant as a rule's, however short. The validation that earns the ruleset (the curation bar — exercise, don't assume) is running prompt-quality against arc's own role directives — does arc over-instruct its own agents? — not a planted rule that only shows the register parses. Open: whether the meta-audit targets the authored role directive or the fully-assembled prompt; how much shared assembly becomes editable Markdown versus staying structural glue.
