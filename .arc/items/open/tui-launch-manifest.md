The launch preview becomes a real view, structured as a manifest — the operator caught it as the TUI's most dated surface: a cramped modal painted over the other boxes (overlapping the masthead, a long warning bleeding into adjacent panes), the rules dumped as wrapped comma prose, and "… N more line(s) — enlarge the terminal" as the overflow story. The reshape: the preview owns the viewport like every other view, and the context renders as sections — the rules block with its count, the README, each included file, the git snapshot, the ledgers — each carrying its size and its share of the assembled prompt (the scope measurement every record already lands), with window-relative percentages joining when context-window-utilization's model→window table lands (that item precedes this one). Sections are enterable with the cockpit's selection model: entering rules lists each rule with its own size, down to the parts. The shaping keys and the fire/cancel contract stay as they are — this reshapes the reading surface, not the launch semantics. The masthead's long-warning bleed fixes with it: a line truncates to its box, never paints a neighbor.
