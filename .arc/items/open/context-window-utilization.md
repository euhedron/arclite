Show context-window utilization — the share of the selected model's window the assembled prompt occupies — in previews, run reports, and TUI views. The measurement reuses the run record's landed scope code at two moments: live on the assembled prompt in a dry-run preview (pre-spend), and stored on the run record after a real run. Window sizes come from a built-in model→window table, settings-overridable like any default (the provider model listings don't carry them); warning rides the threshold facility repo-drift-metrics establishes.
