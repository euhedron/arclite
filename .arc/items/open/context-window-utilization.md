Show context-window utilization — the share of the selected model's window the assembled prompt occupies — in previews, run reports, and TUI views. The prompt's composition (rules vs. content) is measured once, by the run record's landed scope measuring (the `scope` fields' code), at two moments: live on the assembled prompt in a dry-run preview (pre-spend), and stored on the run record after a real run — never re-derived by a second implementation; window sizes come from a built-in model→window table, settings-overridable like any default (the provider model listings don't carry them); warning rides the threshold facility repo-drift-metrics establishes, built once. The deterministic character/token estimate already exists per run.
