Show context-window utilization — the share of the selected model's window the assembled prompt occupies — in previews, run reports, and TUI views. The prompt's composition (rules vs. content) comes from audit-scope-record's structured scope, not re-derived here; window sizes come from a built-in model→window table, settings-overridable like any default (the provider model listings don't carry them); warning rides the threshold facility repo-drift-metrics establishes, built once. The deterministic character/token estimate already exists per run.
