Show context-window utilization — the share of the selected model's window the assembled prompt (rules + content) occupies — in previews, run reports, and TUI views. Window sizes come from a built-in model→window table, settings-overridable like any default (the provider model listings don't carry them); warning rides the threshold facility repo-drift-metrics establishes, built once. The deterministic character/token estimate already exists per run.
