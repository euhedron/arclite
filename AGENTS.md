# Agent Orientation

Codex reads this file as repository guidance. The official Codex `AGENTS.md` docs describe discovery/layering of instruction files, not Claude-style `@` transclusion, so this file uses explicit pointers instead of imports.

Before non-trivial work, read `README.md` for project context and philosophy.

Read `.arc/findings/README.md` for the curated cross-session findings ledger. Treat `agent_session` findings as hypotheses to verify, and `system_run` findings as promoted arclite output with distinct provenance.